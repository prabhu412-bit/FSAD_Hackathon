services:
  - type: web
    name: airline-feedback-system
    env: docker
    dockerfilePath: Dockerfile
    plan: free
    disk:
      name: data-disk
      mountPath: /app/data
      sizeGB: 1
    envVars:
      - key: PORT
        value: 8080
